interface:
  display_name: "提交与 Draft PR"
  short_description: "检查本次变更，推送前整理为一个 commit，经确认后交付 Draft PR 并查看 GitHub CI"
  default_prompt: "使用 $commit 检查本次变更并整理为一个 commit，准备好提交和 PR 说明，在推送前让我确认，默认创建 Draft PR 并报告 GitHub CI 结果。"
