# 使用已登录的 Codex CLI，当前原生协议对应 0.153.4。
# 优先从 PATH 查找；macOS 也查找 Codex/ChatGPT 应用与常见安装目录。
# 手动指定时使用绝对路径；修改后重启 agent-server。
UI_FORGE_CODEX_PATH=
UI_FORGE_CODEX_MODEL=

# 页面与 CLI 共用本机服务。
UI_FORGE_HOST=127.0.0.1
UI_FORGE_PORT=4310

# 运行目录存放服务锁、含设计绑定的会话索引、诊断元数据和上传图片。
# 相对路径以安装根解析；执行历史仍由 Codex 保存。
UI_FORGE_RUNTIME_DIR=.ui-forge/runtime

# 仅 MasterGo + Magic 接入使用；账号还需对应设计访问权限和 MCP 服务权益。
# local 图片/文字和 Vibe 不使用此个人令牌；不代表 Vibe 账号权益永久免费。
# 设计来源与 Magic/Vibe 由每个新任务显式选择，不通过此环境变量设置默认值。
# Vibe 已有本机服务默认 MCP 为 http://127.0.0.1:20678/mcp，
# 状态地址为 http://127.0.0.1:30678/api/status，可在页面或 CLI 接入参数中修改。
MG_MCP_TOKEN=
